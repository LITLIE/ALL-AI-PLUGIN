{
  "mcpServers": {
    "crew": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/server/mcp-server.mjs"],
      "env": {
        "AGENTBUS_PROJECT": "${CLAUDE_PROJECT_DIR}"
      }
    }
  }
}
